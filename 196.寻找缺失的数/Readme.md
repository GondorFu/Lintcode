# GondorFu
---
## 描述
给出一个包含 0 .. N 中 N 个数的序列，找出0 .. N 中没有出现在序列中的那个数。

## 样例
```
N = 4 且序列为 [0, 1, 3] 时，缺失的数为2。
```

## 挑战
在数组上原地完成，使用O(1)的额外空间和O(N)的时间。

## 标签 
贪心

## 解题思路
- 思路：
    
    注意到4的整数倍异或一定为零，比如0-3,4-7,...。因此可以将序列补置4的整数倍，之后依此异或就能得到最后的结果。
    
- 具体操作：

    对数组的依此操作可以使用reduce














